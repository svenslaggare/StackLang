func print(): Void {
	for (Int x = 0; x < 10; x += 1) {
		println(x);
	}

	Int x = 0;
	while (x < 10) {
		println(x);
		x += 1;

		if (x > 4) {
			return;
		}
	}
} 

func abs(Int x): Int {
	if (x < 0) {
		return -x;
	} else {
		return x;
	}
}

func troll(Int x, Int y): Int {
	Int z = x + y;

	if (y > 5 && y < 10) {
		x = x + 2;
	}

	Bool b = y > 5;

	if (!b) {
		return -1;
	}

	return abs(-x) + 5;
}
